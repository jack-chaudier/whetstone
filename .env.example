# Optional server-side secrets. Never prefix these with NEXT_PUBLIC_.
# Copy this file to .dev.vars for the local Worker runtime only.
# GPT Sites production secrets are configured in Sites, not committed here.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# XAI_API_KEY=
